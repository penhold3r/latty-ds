/**
 * Elevation token generation.
 * Provides box-shadow values based on the neutral color palette.
 *
 * @module elevation
 */

export { buildElevation } from './elevation';
export type { ElevationLevel, ElevationTokens } from './elevation';
