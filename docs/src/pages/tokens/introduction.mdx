---
layout: ../../layouts/BaseLayout/index.astro
title: Tokens Overview
description: Design tokens in the Latty Design System
---

import StaticTable from '../../components/StaticTable/index.astro';

# Design Tokens

Design tokens are the single source of truth for every visual decision in Latty. They are shipped as four artifacts:

<StaticTable
  columns={[
    { key: 'output', label: 'Output' },
    { key: 'path', label: 'Import path', code: true },
    { key: 'use', label: 'Use case' }
  ]}
  rows={[
    { output: 'Primitive CSS custom properties', path: '@latty/tokens/tokens.css', use: 'Any browser context' },
    { output: 'Semantic CSS custom properties', path: '@latty/tokens/semantic.css', use: 'Role-based token overrides' },
    { output: 'JSON', path: '@latty/tokens/tokens.json', use: 'Tooling, documentation' },
    { output: 'JavaScript module', path: '@latty/tokens', use: 'JS/TS consumers' }
  ]}
/>

## Token categories

<StaticTable
  columns={[
    { key: 'cat', label: 'Category' },
    { key: 'prefix', label: 'CSS prefix', code: true },
    { key: 'ex', label: 'Example', code: true }
  ]}
  rows={[
    { cat: 'Colors', prefix: '--lt-color-*', ex: '--lt-color-primary-500' },
    { cat: 'Spacing', prefix: '--lt-spacing-*', ex: '--lt-spacing-4' },
    { cat: 'Border radius', prefix: '--lt-border-radius', ex: '0.5rem' },
    { cat: 'Border width', prefix: '--lt-border-width', ex: '1px' },
    { cat: 'Elevation', prefix: '--lt-elevation-*', ex: '--lt-elevation-2' },
    { cat: 'Typography', prefix: '--lt-typography-fontFamilyPrimary', ex: '"Hanken Grotesk", sans-serif' }
  ]}
/>

## Import

#### css

```css
@import '@latty/tokens/tokens.css';
@import '@latty/tokens/semantic.css'; /* optional semantic layer */
```

#### typescript

```js
import '@latty/tokens/tokens.css'; // CSS vars in browser
import { tokens } from '@latty/tokens'; // JS object
```

## Customization

Override any token on `:root` after the import:

```css
@import '@latty/tokens/tokens.css';

:root {
  --lt-border-radius: 2px;
  --lt-typography-fontFamilyPrimary: 'Inter', sans-serif;
}
```

For runtime theming use the `configure()` API — see the [Theming](/getting-started/theming) guide.

## Explore

- [Colors](/tokens/colors) — full palette browser for all semantic colors
- [Spacing](/tokens/spacing) — visual spacing scale
- [Semantic tokens](/tokens/semantic-tokens) — role-based token reference
