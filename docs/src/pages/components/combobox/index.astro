---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import PlaygroundShell from '../../../components/PlaygroundShell.astro';
import FrameworkTabs from '../../../components/FrameworkTabs.astro';
import ApiTable from '../../../components/ApiTable.astro';
---

<BaseLayout title="Combobox" description="Searchable single-select dropdown">
  <lt-text variant="h1">Combobox</lt-text>
  <lt-text variant="body">
    A searchable single-select field. The user types to filter options; selecting one sets the value. Keyboard
    navigation (Arrow, Enter, Escape) is fully supported. Use <code>lt-select</code> when no filtering is needed.
  </lt-text>

  <lt-text variant="h2">Playground</lt-text>
  <PlaygroundShell id="combobox-playground">
    <div slot="preview">
      <lt-combobox id="playground-cb" label="Country" placeholder="Search countries…"></lt-combobox>
    </div>
    <div slot="controls">
      <div class="control-row">
        <label class="control-label" for="ctrl-size">size</label>
        <select class="control-select" id="ctrl-size">
          <option value="sm">sm</option>
          <option value="md" selected>md</option>
          <option value="lg">lg</option>
        </select>
      </div>
      <div class="control-row">
        <label class="control-label" for="ctrl-variant">variant</label>
        <select class="control-select" id="ctrl-variant">
          <option value="default" selected>default</option>
          <option value="success">success</option>
          <option value="warning">warning</option>
          <option value="error">error</option>
        </select>
      </div>
      <div class="control-row">
        <label class="control-label" for="ctrl-disabled">disabled</label>
        <input class="control-toggle" type="checkbox" id="ctrl-disabled" />
      </div>
      <div class="control-row">
        <label class="control-label" for="ctrl-required">required</label>
        <input class="control-toggle" type="checkbox" id="ctrl-required" />
      </div>
      <div class="control-row">
        <label class="control-label" for="ctrl-label">label</label>
        <input class="control-text" type="text" id="ctrl-label" value="Country" />
      </div>
      <div class="control-row">
        <label class="control-label" for="ctrl-placeholder">placeholder</label>
        <input class="control-text" type="text" id="ctrl-placeholder" value="Search countries…" />
      </div>
      <div class="control-row">
        <label class="control-label" for="ctrl-helper">helper-text</label>
        <input class="control-text" type="text" id="ctrl-helper" value="" />
      </div>
      <div class="control-row">
        <label class="control-label" for="ctrl-theme">theme</label>
        <select class="control-select" id="ctrl-theme">
          <option value="" selected>default</option>
          <option value="light">light</option>
          <option value="dark">dark</option>
        </select>
      </div>
    </div>
  </PlaygroundShell>

  <lt-text variant="h2">Usage</lt-text>
  <FrameworkTabs
    html={`
<lt-combobox
  id="country-picker"
  label="Country"
  placeholder="Search countries…"
  required
></lt-combobox>

<script type="module">
  const cb = document.getElementById('country-picker');
  cb.options = [
    { value: 'us', label: 'United States' },
    { value: 'ca', label: 'Canada' },
    { value: 'gb', label: 'United Kingdom' },
    { value: 'de', label: 'Germany' },
    { value: 'fr', label: 'France' }
  ];
  cb.addEventListener('lt-change', (e) => {
    console.log('selected', e.detail.value, e.detail.label);
  });
<\/script>
    `}
    react={`
import { Combobox } from '@latty/react';

const countries = [
  { value: 'us', label: 'United States' },
  { value: 'ca', label: 'Canada' },
  { value: 'gb', label: 'United Kingdom' }
];

<Combobox
  label="Country"
  placeholder="Search countries…"
  options={countries}
  onChange={(value, label) => console.log(value, label)}
  required
/>
    `}
    vue={`
<template>
  <lt-combobox
    label="Country"
    placeholder="Search countries…"
    :options="countries"
    @lt-change="(e) => selected = e.detail.value"
  />
</template>
    `}
  />

  <lt-text variant="h2">API</lt-text>
  <ApiTable tag="lt-combobox" />
</BaseLayout>

<script>
  import '@latty/web';

  const cb = document.getElementById('playground-cb') as any;
  const playground = document.getElementById('combobox-playground')!;
  const preview = playground.querySelector('.preview')!;
  const codeEl = document.getElementById('combobox-playground-code')!;

  cb.options = [
    { value: 'us', label: 'United States' },
    { value: 'ca', label: 'Canada' },
    { value: 'gb', label: 'United Kingdom' },
    { value: 'de', label: 'Germany' },
    { value: 'fr', label: 'France' },
    { value: 'jp', label: 'Japan' },
    { value: 'br', label: 'Brazil' }
  ];

  const state: Record<string, any> = {
    size: 'md',
    variant: 'default',
    disabled: false,
    required: false,
    label: 'Country',
    placeholder: 'Search countries…',
    'helper-text': '',
    theme: ''
  };

  function updateCode() {
    const attrs = Object.entries(state)
      .filter(([k, v]) => {
        if (v === false || v === '') return false;
        if (k === 'size' && v === 'md') return false;
        if (k === 'variant' && v === 'default') return false;
        return true;
      })
      .map(([k, v]) => (v === true ? k : `${k}="${v}"`))
      .join('\n  ');

    codeEl.textContent = attrs
      ? `<lt-combobox\n  ${attrs}\n></lt-combobox>`
      : `<lt-combobox label="Country" placeholder="Search countries…"></lt-combobox>`;
  }

  // size & variant & theme
  ['size', 'variant', 'theme'].forEach((id) => {
    document.getElementById(`ctrl-${id}`)?.addEventListener('change', (e) => {
      const val = (e.target as HTMLSelectElement).value;
      state[id] = val;
      if (val) {
        cb.setAttribute(id, val);
      } else {
        cb.removeAttribute(id);
      }

      if (id === 'theme') {
        if (val === 'dark' || val === 'light') {
          preview.setAttribute('data-preview-theme', val);
        } else {
          preview.removeAttribute('data-preview-theme');
        }
      }

      updateCode();
    });
  });

  // disabled & required
  ['disabled', 'required'].forEach((id) => {
    document.getElementById(`ctrl-${id}`)?.addEventListener('change', (e) => {
      const checked = (e.target as HTMLInputElement).checked;
      state[id] = checked;
      if (checked) {
        cb.setAttribute(id, '');
      } else {
        cb.removeAttribute(id);
      }
      updateCode();
    });
  });

  // label & placeholder & helper
  ['label', 'placeholder', 'helper'].forEach((id) => {
    document.getElementById(`ctrl-${id}`)?.addEventListener('input', (e) => {
      const val = (e.target as HTMLInputElement).value;
      const attr = id === 'helper' ? 'helper-text' : id;
      state[attr] = val;
      if (val) {
        cb.setAttribute(attr, val);
      } else {
        cb.removeAttribute(attr);
      }
      updateCode();
    });
  });

  updateCode();
</script>
